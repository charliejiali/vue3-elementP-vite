interface Settings {
    title: string,
    fixedHeader: boolean,
    sidebarLogo: boolean
}

const settings: Settings = {
  title: 'vue3-elementP-vite',
  fixedHeader: false,
  sidebarLogo: true
}

export default settings
